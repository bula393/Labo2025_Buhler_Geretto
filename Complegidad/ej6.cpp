/* int numerosImparesSeguidos (vector &v) {
    int indice = 0;
    int maxcantNumerosImpares = 0; 
    int cantNumerosImpares = 0 ;
    while ( indice< v.size () ) {                           O(n)
        if (v[indice]%2 != 0) {  O(1)
            cantNumerosImpares++;
        } else
        {
            if ( cantNumerosImpares > maxcantNumerosImpares ) {                     O(1)
            maxMeseta = meseta ;
        }
            
        }
        indice++;
        
    }
    return maxcantNumerosImpares ;
} */